package edu.knoldus.operation;

import java.util.*;

public class ReadSentence {
/*
    ArrayList<ArrayList<Object>> senetenceList = new ArrayList<>();
    Scanner scanner = new Scanner(System.in);

    public void readSentance() {
        System.out.println("enter your sentence");
        String read = scanner.nextLine();
        String[] splited = read.split(" ");
        int count = 0;
        for (int i = 0; i < splited.length; i++) {
            if (isContaining(splited[i])) {
                increaseCount(splited[i]);
            } else {
                add(splited[i]);
            }
        }
    }

    private boolean isContaining(String toCheck) {
    }

    private void increaseCount(String toIncrease) {
        senetenceList.forEach(itemList -> {

            if(toIncrease ==(String)itemList.get(0)){
                ++(int)itemList.get(1);
            }
        });
    }

    private void add(String toAdd) {
        List<Object> listToAdd = Arrays.asList(toAdd, 1);
    }
*/
}
